# Example blog application from class


- run `npm install` to install dependencies
- run `npm run dev` to start the application development serve
- run `npx json-server db.json` to run the json server on port `3000`
